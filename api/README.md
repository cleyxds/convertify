# convert-to-webp

To install dependencies:

```bash
yarn install
```

To run:

```bash
yarn start
```

This project was originally created using `bun init` in bun v1.1.45. It has been updated to use Yarn.
